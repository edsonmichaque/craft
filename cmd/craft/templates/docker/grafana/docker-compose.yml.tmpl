
const grafanaComposeTemplate = `version: '3.8'

services:
  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
    networks:
      - {{.ProjectName}}-network
    volumes:
      - grafana_data:/var/lib/grafana

volumes:
  grafana_data:

networks:
  {{.ProjectName}}-network:
    driver: bridge